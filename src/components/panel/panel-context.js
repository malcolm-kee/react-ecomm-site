import React from 'react';

/**
 * @type {React.Context<'default' | 'primary' | 'success' | 'info' | 'warning' | 'danger' | undefined>}
 */
export const PanelContext = React.createContext(undefined);
