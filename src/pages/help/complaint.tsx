import { ComplainForm } from 'modules/support/components/complain-form';
import { HelpPageWrapper } from 'modules/support/components/help-page-wrapper';
import * as React from 'react';

export default function HelpComplaint() {
  return (
    <HelpPageWrapper>
      <ComplainForm />
    </HelpPageWrapper>
  );
}
